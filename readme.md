## Daily Harvest Take Home Question

Requirements: `Python 3`, `python 3 pip`

#### Run: 
To run the application, simply run
`./boot.sh`

#### Help:
For help or list of options available, type `?` in the command menu